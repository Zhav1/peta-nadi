{
  "name": "peta-nadi-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test:demo": "playwright test e2e/demo-recorder.spec.ts --headed",
    "test:demo:record": "playwright test e2e/demo-recorder.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
