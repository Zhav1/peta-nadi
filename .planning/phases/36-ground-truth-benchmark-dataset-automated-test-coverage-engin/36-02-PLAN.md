---
phase: 36
plan: 02
wave: 2
depends_on:
  - 36-01
files_modified:
  - backend/requirements.txt
  - .coveragerc
  - backend/tests/test_api_routers.py
  - docs/test_matrix.md
autonomous: true
requirements:
  - FR-10.3
  - FR-10.4
---

# Plan 36-02: Pytest Coverage Engine & Comprehensive Test Matrix

<objective>
Configure the test coverage engine (`.coveragerc` and `pytest-cov`), add integration test coverage for FastAPI API routers using TestClient to achieve >=80% core module coverage, and document the complete 45+ test suite inventory in `docs/test_matrix.md`.
</objective>

<execution_context>
- `.planning/phases/36-ground-truth-benchmark-dataset-automated-test-coverage-engin/36-CONTEXT.md`
- `.planning/phases/36-ground-truth-benchmark-dataset-automated-test-coverage-engin/36-RESEARCH.md`
- `backend/requirements.txt`
- `backend/app/main.py`
</execution_context>

<tasks>

<task id="36-02-01">
<read_first>
- backend/requirements.txt
- .planning/phases/36-ground-truth-benchmark-dataset-automated-test-coverage-engin/36-CONTEXT.md
- .planning/phases/36-ground-truth-benchmark-dataset-automated-test-coverage-engin/36-RESEARCH.md
</read_first>
<action>
1. Update `backend/requirements.txt` to include `pytest-cov>=5.0.0`.
2. Create root `.coveragerc` with:
   - `[run]` section:
     - `branch = True`
     - `source = backend/app, agents`
     - `omit = backend/app/adapters/cuopt_adapter.py, backend/app/adapters/fourcastnet_adapter.py, backend/app/adapters/earth2_adapter.py, backend/app/workers/*, */__pycache__/*, */tests/*`
   - `[report]` section:
     - `show_missing = True`
     - `skip_covered = False`
     - `fail_under = 80`
     - `exclude_lines = pragma: no cover, def __repr__, raise NotImplementedError, if __name__ == .__main__.:, pass`
</action>
<acceptance_criteria>
- `backend/requirements.txt` contains `pytest-cov`.
- `.coveragerc` exists at repository root with `branch = True` and `source` pointing to `backend/app` and `agents`.
- Running `backend/.venv/Scripts/python.exe -m pytest --cov-config=.coveragerc` parses the configuration without syntax or option errors.
</acceptance_criteria>
</task>

<task id="36-02-02">
<read_first>
- backend/app/main.py
- backend/app/routers/incidents.py
- backend/app/routers/approvals.py
- backend/app/routers/commodity_router.py
- backend/app/routers/news_router.py
- backend/app/routers/corridor_router.py
- backend/app/routers/vehicles_router.py
- backend/app/routers/health.py
</read_first>
<action>
Create `backend/tests/test_api_routers.py` using `fastapi.testclient.TestClient` to test all primary FastAPI API endpoints.
Implement tests for:
1. `test_health_and_root()`: `GET /health` returns status healthy; `GET /` returns API welcome/version.
2. `test_incidents_endpoints()`: `GET /api/v1/incidents/` returns list of incidents; `GET /api/v1/incidents/live` returns active GeoJSON incidents.
3. `test_approvals_endpoints()`: `GET /api/v1/approvals/` returns route approval records; `POST /api/v1/approvals/` accepts decision payload (decision, route_id, operator_notes) and returns 200/201.
4. `test_commodity_endpoints()`: `GET /api/v1/commodity/` returns commodity price list; `GET /api/v1/commodity/spikes` returns detected anomalies.
5. `test_news_endpoints()`: `GET /api/v1/news/live` returns structured news feed; `GET /api/v1/news/market-regime` returns macro regime status.
6. `test_corridor_endpoints()`: `GET /api/v1/corridor/context` returns multi-source corridor telemetry.
7. `test_vehicles_endpoints()`: `GET /api/v1/fleet/vehicles` returns multi-modal fleet positions across Sumatra.
Ensure mock dependencies or test environment bypasses avoid live external API network calls.
</action>
<acceptance_criteria>
- `backend/tests/test_api_routers.py` exists and contains 7+ endpoint test cases.
- `backend/.venv/Scripts/python.exe -m pytest backend/tests/test_api_routers.py -v` passes 100%.
- Line and branch coverage across core packages (`backend/app` and `agents`) achieves >=80%.
</acceptance_criteria>
</task>

<task id="36-02-03">
<read_first>
- .planning/REQUIREMENTS.md
- backend/tests/test_adapters.py
- backend/tests/test_agents.py
- backend/tests/test_news_pipeline.py
- backend/tests/test_scrapers.py
- backend/tests/test_benchmark_eval.py
- backend/tests/test_api_routers.py
</read_first>
<action>
Create `docs/test_matrix.md` documenting the complete PreHub test suite inventory for judge inspection.
Include:
1. Executive Summary: Total test count (45+), framework (pytest 8.2.2 + pytest-cov), execution time (~15s), branch coverage percentage, and pass rate (100%).
2. Requirement Mapping Table: Mapping every FR (FR-1 through FR-10) and NFR to specific test files and test functions.
3. Detailed Test Catalog Table: Columns for `Test ID` (e.g. `TEST-ADP-01`, `TEST-AGT-01`, `TEST-API-01`, `TEST-BCK-01`), `Target Module`, `Requirement`, `Test Scenario Description`, `Type` (Unit vs Integration), and `Verification Outcome` (PASS).
4. Coverage Breakdown Table: Per-module statement and branch coverage percentages extracted from `pytest --cov`.
5. Reproduction Commands: Exact instructions for judges and evaluators to run the full test suite locally.
</action>
<acceptance_criteria>
- `docs/test_matrix.md` exists and contains formatted Markdown tables.
- All 45+ test cases are individually cataloged with unique Test IDs and requirement tags.
- Document reflects empirical coverage numbers matching the pytest-cov execution report.
</acceptance_criteria>
</task>

</tasks>

<must_haves>
<truths>
- `.coveragerc` is configured and enables branch coverage across `backend/app` and `agents`.
- `backend/tests/test_api_routers.py` tests all major FastAPI endpoints.
- Total pytest test suite contains 45+ passing tests.
- Core package coverage reaches >=80%.
- `docs/test_matrix.md` provides an exhaustive test catalog for judging.
</truths>
<prohibitions>
- No test mocks that bypass internal business logic: tests must exercise the actual endpoint routing and serialization.
- No reliance on active cloud Supabase credentials: all tests must run hermetically with offline fallbacks.
</prohibitions>
</must_haves>

## Artifacts this phase produces
- File: `.coveragerc`
- File: `backend/requirements.txt` (updated)
- File: `backend/tests/test_api_routers.py`
- File: `docs/test_matrix.md`